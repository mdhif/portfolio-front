import { ArticleLayout } from '@/components/ArticleLayout'
import { InputComponent } from '@/components/InputComponent'
import { NewsLetter } from '@/components/InputComponent'
export const meta = {
  author: 'Mohamed Dhif',
  date: '2023-10-02',
  title: 'Text / Speech Models (ASR & TTS)',
  description:
    'This demo showcases the power of the state of the art models in Text / Speech',
}

export default (props) => <ArticleLayout meta={meta} {...props}/>

<NewsLetter/>

```js
import { animate } from '@planetaria/animaginary'

export function MyComponent({ open, children }) {
  return (
    <animate.div
      in={open}
      animateFrom="opacity-0 scale-95"
      animateTo="opacity-100 scale-100"
      duration={350}
    >
      {children}
    </animate.div>
  )
}
```



# Hello World
## this is demo 1
# Test

```python
print("hello world")
```